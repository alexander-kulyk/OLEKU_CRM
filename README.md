# OLEKU_CRM

A CRM built as a pnpm workspace monorepo.

- `client/` — React + TypeScript + Vite, React Router, TanStack Query, React Hook Form, Zod, Tailwind CSS, FullCalendar
- `server/` — Express + TypeScript + Prisma

## Getting started

```bash
pnpm install

pnpm dev:client
pnpm dev:server
```
