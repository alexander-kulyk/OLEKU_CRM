// Public API for the `shared/lib` segment — project-agnostic helpers, such
// as the local date/time <-> instant conversion boundary (design.md D5).
// Empty scaffold from Stage 1 (Foundation); populated in Stage 5.
export {}
